# The client talks to the API via Vite's dev proxy (see vite.config.ts),
# so no API URL is needed for local dev. If you host the API elsewhere,
# set this and update the fetch base in src/api/client.ts.
VITE_API_BASE="/api"
